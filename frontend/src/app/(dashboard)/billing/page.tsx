'use client';

export default function BillingPage() {
    return (
        <div className="p-6">
            <h1 className="text-xl font-semibold">Billing</h1>
            <p className="mt-2 text-sm text-muted-foreground">
                Manage your subscription from the subscribe dialog when inactive.
            </p>
        </div>
    );
}
