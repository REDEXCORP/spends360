import ComingSoon from '@/components/ComingSoon';

export default function ExpensesPage() {
    return (
        <ComingSoon
            title="Expenses"
            description="Monitor operating and capital expenses across departments, projects, vendors, and categories."
        />
    );
}
